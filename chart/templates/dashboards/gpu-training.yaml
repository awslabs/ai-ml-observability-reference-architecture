{{- if .Values.dashboards.enabled }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: ray-train-dashboard
  namespace: {{ .Release.Namespace }}
  labels:
    grafana_dashboard: '1'
data:
  ray-train-dashboard.json: |-
{{ .Files.Get "files/dashboards/ray-train-dashboard.json" | indent 4 }}
{{- end }}
