{{- if .Values.dashboards.enabled }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: ray-vllm-inference-dashboard
  namespace: {{ .Release.Namespace }}
  labels:
    grafana_dashboard: '1'
data:
  ray-vllm-inference-dashboard.json: |-
{{ .Files.Get "files/dashboards/ray-vllm-inference-dashboard.json" | indent 4 }}
{{- end }}
